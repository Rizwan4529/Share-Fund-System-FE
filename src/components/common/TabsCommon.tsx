/**
 * Shared underline tabs — re-exports the project tab primitives.
 * Use these everywhere instead of importing from `@/components/ui/tabs` directly.
 */
export {
  Tabs,
  TabsList,
  TabsTrigger,
  TabsContent,
} from "@/components/ui/tabs";
