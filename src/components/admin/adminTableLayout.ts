/** Shared flex classes for admin table pages (fill remaining shell height). */

export const ADMIN_TABLE_SECTION =
  "flex min-h-0 flex-1 basis-0 flex-col overflow-hidden";

/** h-0 only on the table slot — deepest scroll boundary before DataTable. */
export const ADMIN_TABLE_SLOT =
  "flex h-0 min-h-0 flex-1 basis-0 flex-col overflow-hidden";
